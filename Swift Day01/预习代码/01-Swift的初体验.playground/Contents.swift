//: Playground - noun: a place where people can play

// 1.Swift导入框架是直接使用:import 框架名称
import Foundation

// 2.定义一个标识符时必须制定该标识符时变量还是常量
// 定义标识符的格式:let(常量)/var(变量) 标识符名称 : 数据类型 = 赋值
let age : Int = 20

// 3.语句结束时,可以不跟;
// 注意:如果一行中有多条语句,则依然需要跟;,但是不建议该写法

// 4.打印内容时,不再使用NSLog,而是使用print(打印内容)
print(age)